--- a/gateway/platforms/telegram.py
+++ b/gateway/platforms/telegram.py
@@
     def _should_thread_reply(self, reply_to: Optional[str], chunk_index: int) -> bool:
@@
         else:  # "first" (default)
             return chunk_index == 0
+
+    @staticmethod
+    def _split_paragraphs_for_send(text: str) -> list[str]:
+        """Split assistant replies into readable Telegram messages.
+
+        Keep fenced-code replies intact because Telegram MarkdownV2 code fences
+        are easy to break when split on blank lines. Normal companion replies
+        are split on paragraph breaks so the chat reads naturally on mobile.
+        """
+        stripped = text.strip()
+        if not stripped:
+            return []
+        if "```" in stripped:
+            return [stripped]
+        parts = [part.strip() for part in re.split(r"\n{2,}", stripped) if part.strip()]
+        return parts or [stripped]
@@
-            # Format and split message if needed
+            # Format and split message if needed. Prefer paragraph-sized
+            # Telegram messages for natural chat flow, then fall back to the
+            # hard 4096 UTF-16-unit split for any oversized paragraph.
             formatted = self.format_message(content)
-            chunks = self.truncate_message(
-                formatted, self.MAX_MESSAGE_LENGTH, len_fn=utf16_len,
-            )
+            chunks = []
+            for paragraph in self._split_paragraphs_for_send(formatted):
+                chunks.extend(
+                    self.truncate_message(
+                        paragraph,
+                        self.MAX_MESSAGE_LENGTH,
+                        len_fn=utf16_len,
+                    )
+                )
